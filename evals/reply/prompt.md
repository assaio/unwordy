---
description: A yes/no question answered first and short.
max_turns: 4
---
Is the retry limit configurable? For context: the worker reads sync.retry_limit from config.toml, the default is 3 and the accepted range is 1 to 10.
